// Plantilla -- copia este archivo como "wifi_credentials.h" (mismo folder)
// y pon ahi tus credenciales reales. "wifi_credentials.h" esta en
// .gitignore, así que nunca se sube al repo.
#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

const char* WIFI_SSID = "TU_RED_WIFI";
const char* WIFI_PASSWORD = "TU_CONTRASENA";

#endif
